# 日報 — 2026-04-10

**Google フォーム＋スプレッドシート＋静的フロント（Astro 予定）**への方針を文書化し、リポジトリから **Next.js を撤去**した日。

---

## サマリ

| 項目 | 内容 |
|------|------|
| ドキュメント | **[static-site-strategy.md](../static-site-strategy.md)** を新規作成。確定事項・未決定・旧／新対応表を整理。[functional-spec.md](../old/functional-spec.md) / [system-architecture.md](../old/system-architecture.md) / [README.md](../../README.md) に**参照・注意書き**を追記。 |
| 方針 | フロントは **Astro** を**後学のため**採用予定。**データはシート**、投稿は **Google フォーム**。**axios 不使用**は継続。 |
| コード | **Next.js 一式を撤去**（`package.json`、`src/`、`node_modules`、`.next`、設定ファイル等）。**Astro は未追加**。 |
| `doc/old/supabase/` | マイグレーションは**リポジトリに残置**。[supabase/README.md](../old/supabase/README.md) に「現方針では参考」旨を追記。 |

---

## 主な作業内容

### 1. `static-site-strategy.md`

- 運用：**X 専用アカウント**、<strong>フォーム → 受付シート →（手動）掲載用</strong>、削除はシート＋依頼チャネル。
- フォーム項目：**投稿者名、投稿祭名、X 任意、ニコ動URL 必須、開始／終了、カ**テゴリ選択。
- フロント計画：**カード一覧**、<strong>カテゴリ・開催月</strong>での表示制御、<strong>利用規約</strong>・フォームリンク、<strong>ニコ動URL からサムネ組み立て</strong>（失敗時は代替）。
- **未決定**：公開CSV vs Apps Script JSON、シート構成、サムネURL規則の確定、ホスティング、Astro の配置パス等。
- **リポジトリ現状**の記述は、<strong>Next 撤去コミット後</strong>に「撤去済み・Astro 未追加」へ更新済み。

### 2. 既存ドキュメントとの役割分担

- **正（プロダクト意思決定）**: `static-site-strategy.md`
- **旧・参照**: `doc/old/functional-spec.md`（Supabase / Discord / 自前フォーム等）、`doc/old/system-architecture.md`（Next + Supabase 図・RLS・管理者認定の記録）

### 3. Next.js 撤去（リポジトリ整理）

削除対象の例：

- `src/`、`public/`、`package.json`、`package-lock.json`、`node_modules/`、`.next/`
- `next.config.ts`、`next-env.d.ts`、`tsconfig.json`、`postcss.config.mjs`、`tailwind.config.ts`、`eslint.config.mjs`

残置：

- `doc/`、`doc/old/supabase/`、`.gitignore`、`.env.example`（将来用のコメント例へ差し替え）、更新済み `README.md`

---

## Git 履歴（本日・新しい順）

```
4cb147c Next.jsを撤去
8cc7e7d 構成を変えたため、経緯と結果をドキュメントに追加
```

詳細（ハッシュ・日付・作者・件名）：

```
4cb147c 2026-04-10 Yutaka Shimada Next.jsを撤去
8cc7e7d 2026-04-10 Yutaka Shimada 構成を変えたため、経緯と結果をドキュメントに追加
```

---

## 次回 TODO（参考）

1. **Astro** プロジェクト追加（`npm create astro@latest` 等）、配置はルートか `web/` 等を決める。
2. **掲載用スプレッドシート**の列定義・公開 CSV または GAS JSON の決定。
3. **ニコ動サムネ** URL 規則の実装前確認とフォールバック。
4. **Google フォーム**・専用 **X** の開設とリンク集約。
5. 静的ホスティング（GitHub Pages / Cloudflare Pages 等）の選定。

---

## 関連ドキュメント

| ドキュメント | 内容 |
|--------------|------|
| [static-site-strategy.md](../static-site-strategy.md) | 現行方針の一括参照 |
| [README.md](../../README.md) | リポジトリ概要（Next 撤去済み明記） |

---

**改訂**: 初版（2026-04-10）
