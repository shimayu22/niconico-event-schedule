# 日報 — 2026-04-04

開発セッションは**短め**。主に **HTTP クライアント方針の整理**と **ローカルで Next.js が動くことの確認**。

---

## サマリ

| 項目 | 内容 |
|------|------|
| 主な成果 | **axios を使わない**方針を README / アーキテクチャ / 仕様の改訂履歴に明記。**`fetch`** 用の薄いヘルパ [`src/lib/http/fetch-json.ts`](../../src/lib/http/fetch-json.ts) を追加。 |
| 実行環境 | **Volta** で Node が未導入だったが、`volta install node@lts`（結果 **Node 24.14.1**）で解消。`npm install`（368 パッケージ、脆弱性 0）、`npm run dev` で **Next.js 15.5.14（Turbopack）** が起動し **`/` が 200** で表示された。 |
| 未実施 | **Supabase** プロジェクト作成・マイグレーション適用・`.env.local` の本番値（**次回の主タスク**とする）。 |

---

## 議論・決定

| 内容 |
|------|
| **axios** を直接・間接ともに使わない（攻撃・サプライチェイン懸念を踏まえた方針）。HTTP は **標準 `fetch`** と **@supabase/supabase-js / @supabase/ssr** に限定。追加依存後は **`npm ls axios`** で確認する運用を README に記載。 |
| **`create-next-app` の再実行**は、既に手動で足した足場と競合しうるため **リポジトリ直下では不要**と回答（`npm run dev` で十分）。 |

---

## 参考・トラブルシュート

| 事象 | 対処 |
|------|------|
| `Volta error: Node is not available` | `volta install node@lts`（またはプロジェクトで `volta pin node@20` 等） |

---

## 今日触った／確認した成果物

| 種別 | パス・内容 |
|------|-------------|
| 方針 | [README.md](../../README.md)（HTTP / axios 節）、[system-architecture.md](../system-architecture.md)、[functional-spec.md](../functional-spec.md) 改訂履歴 |
| コード | [src/lib/http/fetch-json.ts](../../src/lib/http/fetch-json.ts)、既存の Next / Supabase クライアント足場 |

---

## Git 履歴（参照時点・新しい順）

```
6885567 Next.jsのセットアップ
c7060d7 ドキュメントを更新
c405099 日報作成
afe3edd 仕様を詰めている（途中）
05f2a03 ディレクトリ名変更
5fa202e 利用技術を選定
17cf8d9 first commit
```

（コミット作者・日付の詳細は `git log` で確認）

---

## 次回 TODO（想定：Supabase セットアップ）

1. Supabase でプロジェクト作成。
2. [supabase/migrations/20260402120000_init_schema.sql](../supabase/migrations/20260402120000_init_schema.sql) を **SQL Editor で適用**（または CLI）。
3. `private.admin_config` の **`CHANGE_ME@example.com` を運用メールに更新**（[supabase/README.md](../supabase/README.md)）。
4. Auth：**一般サインアップ無効**、管理者ユーザのみ作成（アーキテクチャ「管理者の認定」）。
5. ルートに **`.env.local`**（`NEXT_PUBLIC_SUPABASE_*`、`SUPABASE_SERVICE_ROLE_KEY` 等）を反映。
6. アプリから **一覧 SELECT** が通るか軽く確認（任意）。

---

## 関連ドキュメント

| ドキュメント | 内容 |
|--------------|------|
| [supabase/README.md](../supabase/README.md) | マイグレーション適用・管理者メール・Auth |
| [system-architecture.md](../system-architecture.md) | RLS・認定・fetch 方針 |

---

**改訂**: 初版（2026-04-04）
